server.port=${PORT:8080}

spring.cloud.gcp.sql.database-name=card_reader_db
spring.cloud.gcp.sql.instance-connection-name=sonorous-zone-294814:europe-west3:card-reader-aac9d6a4

# So app starts despite "table already exists" errors.
spring.datasource.continue-on-error=true
# Enforces database initialization
spring.datasource.initialization-mode=always

# Leave empty for root, uncomment and fill out if you specified a user
spring.datasource.username=admin

# Uncomment if root password is specified
spring.datasource.password=szakdoga123

#spring.cloud.gcp.project-id=
#spring.cloud.gcp.credentials.location=file:/path/to/service-account.json
